INPUT:
  SIZE: (224, 224)
  TRANSFORMS: ["random_flip", "random_translation", "normalize"]

DATASET:
  NAME: "SSDGTerra"
  NUM_LABELED: 150

MODEL:
  BACKBONE:
    NAME: "resnet18"
    PRETRAINED: True